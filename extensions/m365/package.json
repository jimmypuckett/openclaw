{
  "name": "@spinen/openclaw-m365",
  "version": "2026.6.11",
  "private": true,
  "description": "OpenClaw Microsoft 365 Graph tools for scoped mail and calendar access.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "compat": {
      "pluginApi": ">=2026.6.10"
    },
    "build": {
      "openclawVersion": "2026.6.11"
    }
  }
}
